GOOGLE_API_KEY=your_gemini_key_here

# Only needed to run scripts/fetch_india_mpa_data.py (see PROJECT_CONTEXT.md
# Section 14o). Request one at https://api.protectedplanet.net/request -
# approval is manual, not instant (same as MOSDAC's original account
# process - Section 13a). Not required to run the main app.
PROTECTED_PLANET_API_TOKEN=your_protected_planet_api_token_here
